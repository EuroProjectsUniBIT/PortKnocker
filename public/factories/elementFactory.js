'use strict';

angular.module('myApp')
  .factory('ElementFactory', ['$scope', function($scope) {
    var factory = {};
    factory.addElement = function(parent, child) {
      
    };
    return factory;
  }]);
